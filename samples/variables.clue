

` Program that demonstrates variables.
x := 3;
y := 4.2;
z := x + y * 5;

` In clue, when you write an expression with a 'missing' lvalue,
` (it looks like this expression isn't being used for anything)
` it gets written to stdout.
x + y + z;

